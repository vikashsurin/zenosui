export const base: "zu_button btn inline-flex justify-center items-center  py-[0.5em] px-[0.75em] gap-[0.5em]";
export namespace btn_themes {
    let light: string;
    let dark: string;
}
export namespace btn_variants {
    let solid: string;
    let filled: string;
    let outlined: string;
    let dashed: string;
    let ghost: string;
    let link: string;
}
