<script lang="ts">
	import { IconButton } from '$lib/index.js';
	import BadgeCheck from '@lucide/svelte/icons/badge-check';
</script>

<div class="">
	<div class="grid gap-2 py-4">
		<IconButton uiSize="xs" icon={BadgeCheck} />
		<IconButton uiSize="sm" icon={BadgeCheck} />
		<IconButton uiSize="md" icon={BadgeCheck} />
		<IconButton uiSize="lg" icon={BadgeCheck} />
		<IconButton uiSize="xl" icon={BadgeCheck} />
		<IconButton uiSize="2xl" icon={BadgeCheck} />
		<IconButton uiSize="3xl" icon={BadgeCheck} />
		<IconButton uiSize="4xl" icon={BadgeCheck} />
		<IconButton uiSize="5xl" icon={BadgeCheck} />
		<IconButton uiSize="6xl" icon={BadgeCheck} />
		<IconButton uiSize="7xl" icon={BadgeCheck} />
		<IconButton uiSize="8xl" icon={BadgeCheck} />
	</div>
</div>
